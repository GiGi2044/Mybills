<nav class="navbar">
  <div class="nav-title">
    <% if user_signed_in? %>
        <%= link_to bills_path() do%>
          <%= image_tag "logo.png", size: "50x50", class: "rounded-circle" %>
        <% end %>
      <span class="a">MyBills</span>
      <span class="a"><%= current_user.email %></span>
      <a href="<%= bills_path %>" class="a">My bills</a>
      <a href="<%= edit_user_path(current_user) %>" class="a">Edit Profile</a>
      <a href="<%= destroy_user_session_path %>" class="a" data: { turbo_method: :delete }>Log out</a>
    <% else %>
      <a href="#" class="a">Guest User</a>
      <a href="<%= new_user_session_path %>" class="a">Log in</a>
      <a href="<%= new_user_registration_path %>" class="a">Sign Up</a>
    <% end %>
  </div>
</nav>
